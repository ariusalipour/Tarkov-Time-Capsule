import { ApplicationCommand } from "@glenstack/cf-workers-discord-bot";

export const bossCommand: ApplicationCommand = {
	name: "boss",
	description: "Bot will get the map spawn rates for the boss you specify!",
};
